// Small, app-wide (non-visual) constants that don't belong in theme.ts. Kept separate so
// theme.ts stays purely about colors/spacing/fonts.

// The canonical public URL used anywhere the app references itself (e.g. share text) —
// defined once here rather than scattering the literal string across screens.
export const APP_URL = 'https://apparentlyyou.com';
